{
  "installCommand": "git config --global url.\"https://github.com/\".insteadOf git@github.com: && pnpm install --ignore-scripts",
  "rewrites": [{ "source": "/(.*)", "destination": "/" }],
  "github": {
    "enabled": true
  }
}
